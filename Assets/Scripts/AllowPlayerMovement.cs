using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AllowPlayerMovement : MonoBehaviour {

    /*PlayerController playerController;
    Vector2 tempPlayerTargetPosition;

    void Start()
    {
        playerController = GameObject.FindWithTag("Player").GetComponent<PlayerController>();
    }

    void OnMouseDown()
    {
        Debug.Log("clikced on ground");
        playerController.isMoving = true;
        tempPlayerTargetPosition = Input.mousePosition;
        playerController.targetPosition = Camera.main.ScreenToWorldPoint(tempPlayerTargetPosition);
    }*/
}
